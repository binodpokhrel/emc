package com.bin.easymobilecare.util;

/**
 * Created by pranishshrestha on 5/2/17.
 */

public class AnalyticsUtil {

   /* public static void addView(Context context, String screenName) {
        if (BuildConfig.ENABLE_ANALYTICS) {
            Tracker tracker = getTracker(context);
            tracker.setScreenName(screenName);
            tracker.send(new HitBuilders.ScreenViewBuilder().build());
        }
    }*/


   /* private static Tracker getTracker(Context context) {
        Activity activity = (Activity) context;
        Application application = activity.getApplication();
        nestedrecycler sipradi = (nestedrecycler) application;
        return sipradi.getDefaultTracker();
    }*/


}
